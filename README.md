A simple service for transmitting census data:

    1. we transform the data,
    2. store it
    3. serve it all through a single endpoint

To start the service, use this command:

    sh bin/init.sh
